CREATE TABLE country (
    code TEXT NOT NULL PRIMARY KEY,
    name TEXT NOT NULL
);

CREATE INDEX countries_code ON country(code);

insert:
INSERT INTO country (code, name) VALUES (:code, :name);

selectAll:
SELECT * FROM country;

deleteAll:
DELETE FROM country;
